try {
    node {
        stage('Snyk Scan') {
            catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
                snykSecurity organisation: 'aldotrucios', projectName: '${JOB_NAME}', severity: 'high', snykInstallation: 'snyk@latest', snykTokenId: 'organisation-snyk-api-token', targetFile: '.'
            }
        }
    }
} catch (Exception e) {
    node {
        throw e
    }
}
